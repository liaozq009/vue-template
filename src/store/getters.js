const getters = {
  // menuDialogVis: state => state.form.menuDialogVis,

  // name: state => {
  //   if (state.app.name) {
  //     return state.app.name
  //   }
  //   return 'CRM'
  // }
}
/**
 * 使用说明
 * import { mapGetters } from 'vuex'
 * computed: {
    ...mapGetters([
      'userInfo'
    ])
  }
 */

export default getters
